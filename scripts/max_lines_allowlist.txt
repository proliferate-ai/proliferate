# Existing oversized source files. Keep this list shrinking over time.
#
# Format:
# path max_lines reason
#
# max_lines is the observed line count allowed for this file. If the file
# grows, the check fails. If it shrinks, update the count or remove the entry
# when the file is under its threshold.

anyharness/crates/anyharness-contract/src/v1/events.rs 1092 existing AnyHarness contract oversized file
anyharness/crates/anyharness-contract/src/v1/sessions.rs 696 existing AnyHarness contract oversized file (+caller-selected idempotent create id; stale fingerprint reduced after legacy public subagent response removal)
anyharness/crates/anyharness-lib/src/api/openapi.rs 683 existing AnyHarness oversized file (+hosting schema registration, +agent-auth state response, +gateway catalog endpoints) + enriched gateway-models schema fields (model-table §1) + workflow-runs + local materialization operations + workflow-run-workspaces paths/schemas + agent install progress schemas + worktree restore path/schemas + WorktreeBaseFetch + RuntimeDiskPressure schemas (subagent registrations extracted to their owned OpenAPI document) ; -10: R1 archiving deletes the three retire path registrations, the three worktree-retention path registrations and the three WorkspaceRetire* schema registrations ; +22: R4 archiving registers the two lifecycle paths and the fourteen lifecycle schemas ; -4: R5 deletes the purge-preflight/retry path registrations and the WorkspaceCleanupOperation/WorkspacePurgePreflightResponse schema registrations ; -30: workflows gen-2 PR1 deletes the gen-1 workflow-runs and workflow-run-workspaces path/schema registrations ; +2: PR5a merges the workflow-runs OpenAPI document into the composed doc ; +16: agent-auth canonical evidence model registers the thirteen AgentAuth* schemas
anyharness/crates/anyharness-lib/src/api/router_tests.rs 1898 existing AnyHarness oversized test file (+hosting PR-status route tests, +catalog version tests, +managed default-placement route test, +model-snapshot status route shape/auth tests, +agent-auth/install poke contract tests, +agent-auth/install poke-suppression proofs against the automatic_poke_engine seam (F-036 cycle-2 review item 1); the catalog-push handler tests are gone with the heartbeat transport)
anyharness/crates/anyharness-lib/src/domains/agent_operations/runtime/error.rs 618 agent-operations error mapping + outcome classifier (+18: #1849 review fixes classify WORKSPACE_ARCHIVED / SUBAGENT_FANOUT_LIMIT / WORKSPACE_SINGLE_SESSION as denied and extend the classifier test)
anyharness/crates/anyharness-lib/src/domains/workflows/store.rs 1327 workflows gen-2 durable store: run/node/doc writes, apply_transition and projections share one SQLite seam; split into store/ on next growth per guides/domains.md ; +62: PR3 adds the docs-registry register/read seam and rendered-envelope persistence ; +32: PR4 adds the Ruling L DisposeThenStart side effect, the node model column, and queue-aware hold event emission ; +115: PR5a adds the courier PUT upsert (Ruling B one-live-run refusal, key-sorted definition JSON note) and the node_membership 404 discriminator read ; +11: pr11 adds the failure_code classification to the named transition event ; +20: pr11b splits the queued-interjection Hold out of the stale-notification target
anyharness/crates/anyharness-lib/src/domains/workflows/store_tests.rs 1462 workflows gen-2 store suite over real migrations (idempotent create, transition application, projections, boot fence, docs registry) ; +139: PR3 adds docs-registry and rendered-envelope round-trip proofs ; +31: PR5a adds one-live-run refusal and membership-discriminator proofs ; +79: pr11 adds the subscriber-capture proof for the named transition event ; +89: pr11b adds the shared capture harness plus interjection-held and node_launched join-key proofs
anyharness/crates/anyharness-lib/src/domains/workflows/transition.rs 738 workflows gen-2 pure transition table: the ADR's ruling rows and their guards in one match ; +27: PR4 adds the ForcedUnload stop reason (parks as app_shutdown) and Ruling L redo-from-running
anyharness/crates/anyharness-lib/src/domains/workflows/transition_tests.rs 1103 workflows gen-2 transition suite: one test per ADR table row plus staleness and illegal-command proofs ; +76: PR4 adds ForcedUnload-parking and Ruling L redo-from-running proofs
anyharness/crates/anyharness-lib/src/domains/agents/catalog/projection.rs 998 family normalizer (bedrock/date/version forms) with tests (+native-default-model resolution and its tests, incl. the opencode any-non-gateway-fallback pin), plus the enrichment join (own vs foreign catalog match) and its tests relocated from agent_gateway_catalog.rs under A9's route-backend swap (ruling §3): gateway_resolver.rs is deleted (old resolver chain, its bedrock_default_model catalog-default resolution now lives in gateway_plan.rs), and the route file itself dropped to 312 lines and no longer needs an allowlist entry
anyharness/crates/anyharness-lib/src/domains/agents/catalog/validation.rs 609 existing catalog validator router; unattended-mode invariants live in a focused submodule
anyharness/crates/anyharness-lib/src/domains/agents/route_auth/render.rs 825 Track D provider_config render arm (claude/opencode generic env-set, codex aws_bedrock via built-in amazon-bedrock provider, codex azure_openai UNVERIFIED config.toml injection kept behind the registry's pending flag, cursor/grok/unknown-kind UnsupportedRoute) landed alongside the existing native/gateway recipe table, +sanitize_claude_ambient fix so a provider_config route's own rerouting flag (e.g. CLAUDE_CODE_USE_BEDROCK) survives sanitization instead of being unconditionally stripped, scoped in review cycle 1 to the keys the provider_config arm itself recorded so an api_key row named like a rerouting flag is still stripped (D3-rust)
anyharness/crates/anyharness-lib/src/domains/agents/auth/context_tests.rs 645 classifier tests incl. gateway route activation + bedrock flag-only detection
anyharness/crates/anyharness-lib/src/domains/agents/auth/launch_facts.rs 657 Track D provider_config credential facts: a source's already-resolved env map classifies per-key against the registry's declared flag vocabulary (registry_flag_vars), producing EnvFlag for mode-switch flags (e.g. CLAUDE_CODE_USE_BEDROCK) and presence-only Env for everything else, same secrets rule as api_key; the vocabulary unions auth.slots[] with providerConfig[] (D3-rust)
anyharness/crates/anyharness-lib/src/domains/cowork/runtime.rs 1893 existing AnyHarness oversized file (catalog-owned unattended default replaces local fallback helper)
anyharness/crates/anyharness-lib/src/domains/plans/service.rs 607 existing AnyHarness oversized file; shrank from 628 after folding plans/plan_interaction_links reads into PlanStore (grid plan PR 5b)
anyharness/crates/anyharness-lib/src/domains/goals/service_tests.rs 682 goal service regression tests for pending_op idempotency
anyharness/crates/anyharness-lib/src/domains/reviews/service/service_tests.rs 1510 existing AnyHarness oversized test file
anyharness/crates/anyharness-lib/src/domains/sessions/runtime/prompt.rs 609 session prompt dispatch ; +39: workflows gen-2 PR4 adds send_text_blocks_prompt_with_id (the workflow envelope's multi-block twin) sharing one send_payload_prompt_with_id seam with the text path
anyharness/crates/anyharness-lib/src/domains/sessions/runtime/prompt_message_actor_tests.rs 601 scripted-ACP prompt/queue race suite over the real actor ; +2: workflows gen-2 PR4 teaches the scripted agent program a refusal stop-reason branch
anyharness/crates/anyharness-lib/src/domains/sessions/runtime/tests.rs 1019 existing AnyHarness oversized test file (launch-env tests moved to their owning module) + A9 Scope C resume/fork revoked-credentials regression tests (AgentNotReady at the common live-start seam) + a review-fix pin (unsatisfiable selection reports RouteAuth, not AgentNotReady, on resume)
anyharness/crates/anyharness-lib/src/live/sessions/probe.rs 679 catalog probe with per-harness model-source fallbacks (+the three runtime-probe options: env removal, scratch workspace root, config-option-only switch suppression); shrank from 683 after the readiness resolve moved to the call boundary (grid plan PR 5c/7 — live never fetches)
anyharness/crates/anyharness-lib/src/live/workflows/lifecycle_tests.rs 1070 workflows gen-2 engine lifecycle suite: scripted-ACP agents drive real runs through the manager/actor (happy path, gate approve, flip, redo, undo, boot fence) with zero mocks of our machinery
anyharness/crates/anyharness-lib/src/live/terminals/manager_tests.rs 730 R3 workspace-stop terminal suite: +615 lines of stop_all_for_workspace coverage (kill census, setup-process reaping, wedged-terminal escalation) sharing one real-PTY manager harness per case
anyharness/crates/anyharness-lib/src/persistence/custom_migrations.rs 1318 custom migration with legacy schema coverage (+0063 partial unique controller index, ordered after the 0062 rebuild) ; +110: R1 archiving adds the 0067_workspace_archived_lifecycle FK-off full-table rebuild ; +72: R5 adds the 0068_workspace_drop_cleanup_columns FK-off full-table rebuild ; +10: workflows gen-2 PR1 registers the 0069_workflow_runs_gen2 FK-off rebuild
anyharness/crates/proliferate-diagnostics-client/src/fallback/tests.rs 736 Fallback writer suite: every test drives one writer through the same rotation and live-authority harness (injected write errno, short writes, rotation, retirement, consecutive-failure retirement), so the shared harness is the reason it is one file and the tests themselves are independent. Split into fallback/tests/{write,rotation,authority}.rs when the next injection seam lands.
anyharness/crates/proliferate-diagnostics-client/src/fallback/unix.rs 651 Unix fallback writer: segment rotation, live-authority validation, partial-line rollback, and bounded interrupt-retrying writes all mutate the same file handle and byte counters under one lock, so the retry path cannot move without exporting that state. The cross-platform swap primitive already moved out to fallback/swap.rs; split the authority validation out next when a second platform needs it.
anyharness/sdk-react/src/hooks/sessions.ts 675 existing SDK React oversized file (subagent queries and lifecycle mutations extracted to their owned hook module)
apps/desktop/src-tauri/src/diagnostics/support_snapshot/artifact_store/unix_fs.rs 658 Support snapshot artifact store's Unix filesystem layer: private-by-construction root creation, metadata repair, and the O_NOFOLLOW open/read/write paths share the same permission constants and repair state machine, and the mkdir-mode hardening this slice adds is only meaningful next to the repair it guards. Split into unix_fs/{root,entry}.rs when a second artifact kind lands.
apps/desktop/src-tauri/src/diagnostics/support_snapshot/scrub/patterns.rs 610 Support export scrub pattern table: the labelled, unlabelled-scheme, environment-assignment, provider-prefix, and opaque-catch-all rules form one ordered redaction pipeline whose precedence is load-bearing and pinned by the scrub tests, so a rule family cannot move without moving that order with it. Split into scrub/patterns/{labelled,unlabelled,opaque}.rs when a sixth rule family lands.
apps/desktop/src-tauri/src/diagnostics_collector/broker/server.rs 672 Desktop diagnostics broker server: the accept loop, per-request admission, and the bounded connection/finite/tail/export permits all share one listener, one shutdown watch, and one BrokerLimits value that the handlers mutate in place, so no handler moves out without duplicating that state. Split into broker/server/{accept,admission,handlers}.rs when the next request type lands.
cloud/sdk/src/types/generated.ts 795 existing generated cloud SDK type surface (+workspaceKind, git numstat fields, OpenAPI-derived materialization types, app-extra overlay, and PR 5 exact-ref create request fields; -actionBlock fields; + lostAt (C7))
anyharness/sdk/src/reducer/__tests__/transcript.test.ts 1721 existing SDK oversized test file; legacy Subagents MCP reducer coverage removed in PR10
anyharness/sdk/src/reducer/transcript.ts 1515 existing SDK oversized file; legacy Subagents MCP reducer branch removed in PR10
apps/desktop/src-tauri/src/commands/ssh_tunnel.rs 729 existing desktop native oversized file
server/proliferate/auth/identity/store.py 695 managed sandbox GitHub grant read path extends existing identity store + SG10 review: explicit crypto-secret calls retain the original readable grant flow instead of line-neutral compression
server/proliferate/config.py 621 managed Cloud, GitHub App, and Workflow flags extend the centralized deployment settings surface; split pending; +7: publisher-lane agent_catalog_artifact_base_url/agent_catalog_channel settings (Update Flow ADR, FR-1)
server/proliferate/db/models/cloud/agent_gateway.py 643 C-train composition: C-2 agent_auth_delivery_ack model joins the B/D cut; agent_gateway_enrollment_key model added for per-(subject,harness) scoped virtual keys (B2), then the agent_model_snapshot re-key (B4) documented why its scope carries NO unique key and what that costs the reader (the active row is plural in principle, so reads tie-break on id); AgentApiKey gains a kind column (D1); a module split is deferred to the model-taxonomy pass rather than done mid-stack; typed-config write gate loosens ck_agent_auth_selection_api_key_shape with a why-not-tighter comment; SG12 keeps the base docstring intact while the direct wall-clock owner import adds one line
server/proliferate/server/accounts/identity/api.py 514 desktop-surface provider callbacks now dispatch to an HTML handoff/error page instead of a raw 302 via a new _provider_callback_response helper (PRO-111); split pending
server/proliferate/server/billing/stripe_webhooks.py 665 W-F2 period-boundary gate adds one guard clause (a billing_reason check plus its drop report and the hold-clear that must still run) to _handle_invoice_paid; the reporter and its context assembly live in webhook_drops.py. W-F5 drop visibility: the intake path was already 595/600 before this change, so threading the Stripe event id through the five handlers and adding one report call per silent early return cannot fit under the cap. The drop vocabulary, levels, paid-detection, and all per-reason context assembly were split out to webhook_drops.py to keep the growth to call sites only; splitting the handlers themselves is deferred to the billing service taxonomy pass rather than done inside an observability-only fix. Adversarial-review fix pass (MAJOR 1) adds a two-line reporter call at the dispatch fall-through's 13th silent drop (unhandled_event_type); billing error normalization changes only the direct error import
server/proliferate/server/cloud/materialization/sandbox_io/connect.py 613 sandbox connect/ready choreography (attempt CAS, usage convergence) + gateway-cache invalidation at the re-stamp (C8); split pending + SG10 review: explicit crypto-secret calls retain the original lifecycle spacing instead of line-neutral compression
server/proliferate/server/cloud/workspaces/service.py 1173 workspace materialization ledger service (intent/report/unlink write-path + primary selection + recorded-sandbox reconciliation + scratch/repo-less read guards) + PR 5 exact-ref managed-Cloud creation and association path + cold-access repairing runtime-access resolution (S2); AnyHarness provisioning handoff split out per guides/domains.md; + git_identity_required translation (C3) + after-commit worktree retire and lost-status derivation (C7) + worker-staleness degraded surfacing (_worker_degraded, S3)
server/tests/unit/test_cloud_connect_race.py 605 connect/detach commit-ambiguity race suite (usage-close fencing fakes) + mark-lost stub wiring (C7)
server/tests/unit/test_cloud_webhook_service.py 610 E2B webhook receipt suite (killed/paused/resumed/timeout receipt + usage-segment fencing) + marked-lost stamping assertions (C7)
server/tests/unit/auth/test_identity_error_ownership.py 603 PRO-111 adds the surface/error field assertions to the OAuthProviderTokenRejectedError redirect-shape test now that complete_oauth_provider_callback returns AuthCallbackRedirect instead of a bare url string; split pending
server/tests/integration/test_agent_gateway_api.py 1242 C-train composition; P1 auth rebuild rewrote keys/selections/state coverage for titled-key + selection model; state coverage now seeds a per-harness child key (B3 renderer); C1 adds cursor gateway-rejected/api_key-accepted (local + cloud) selection cases, plus the disabled gateway-marker-row asserts restored on review fix; +provider-config kind coverage (D1); +per-kind required-field validation coverage (D1 fix cycle, review finding 6); +structural route-shape pin for the S1 /agent-auth vs /agent-gateway split (cycle-2 fix) D train rewrites the enrollment-shape cases org-only (D2) and drops the personal-subject fixtures; typed-config write gate adds typed-selection PUT coverage (open combos, pending/undeclared refusals, A6 shape pins, R5 azure field-set pin, A5 write-to-render state case); split pending with the suite taxonomy pass
server/tests/integration/test_agent_gateway_store.py 714 store-level agent_gateway suite; +create_agent_provider_config/get_agent_provider_config_decrypted kind-scoped fetch symmetry coverage (D1) D-3 deletes the personal-enrollment discovery test, adds the migration feed coverage; typed-config write gate reworks the bad-shape case for the kind-aware gate
server/tests/integration/test_cloud_workspace_materialization_service.py 1190 durable materialization ledger service suite (intent/report/unlink transitions, redaction, primary selection, recorded-sandbox reconciliation, repo-less workspace, install-id redaction, generation replay/crash-retry/terminal-reintent) + PR 5 exact-ref create coverage; new fail-closed source cases split to test_cloud_workspace_exact_ref_source.py; + lost-workspace rematerialization rejection (C7)
server/tests/integration/test_auth_flow.py 2331 existing server oversized integration test plus session-token-revocation coverage + /users/me password rejection test + single-org current_product_user bypass coverage + SG10 review: explicit crypto-secret fixture setup retains the nested grant construction; PRO-111 desktop handoff/error page coverage (launch script + provider error page + web-stays-302 pin) adds three tests to the existing desktop/mobile provider callback surface; PRO-111 review fixes: M2 folds the launch-script test into a parse-deep-link-then-exchange assertion, L5 tightens the error-page tone assertion
server/tests/integration/test_billing_accounting.py 872 existing server oversized integration test
server/tests/integration/test_billing_api.py 1944 existing oversized billing integration suite; billing error normalization adds exact parent-router, nested-router, and permission-denial envelope characterization
server/tests/integration/test_billing_fail_closed_read_errors.py 693 billing error normalization characterizes both snapshot-read and verdict-persistence inheritance corridors in the existing fail-closed integration fixture
server/tests/integration/test_stripe_webhooks.py 1502 existing server oversized integration test; W-F2 adds billing_reason to two invoice fixtures that would otherwise take the new non-boundary early return (silently un-testing the pro-pricing gate and the boundary hold-clear they exist to cover), plus an annotation on the seat-reconciliation grant assertion. That annotation is the larger part of the growth and is deliberate: the assertion pins 15.0 h, which review established is a real over-grant (~17.5 vs ~12.5 owed) reached by re-delivery of a boundary invoice after a seat was added mid-period — the same arithmetic as W-F2 through a different door. It is pinned as current behaviour with the residual named and its narrow reachability stated, rather than left reading as correct, because an in-repo comment blessing this shape is how the W-F2 bug survived review the first time; billing error normalization pins the direct product-error envelope after route-wrapper removal
server/tests/integration/test_stripe_webhook_drop_visibility.py 685 W-F5 level-policy suite; W-F2 adds the INVOICE_NOT_PERIOD_BOUNDARY level case here rather than in its own file because the assertion under test is precisely that this drop is NOT confused with INVOICE_GRANT_GATE_CLOSED, whose level case sits directly above it, and both need this module's caplog/propagate and report_critical fixtures. The file was already 597/600 before the addition. A split by drop family is deferred to the billing suite taxonomy pass
# Relocated from apps/desktop/src by the Desktop-product move into product-client
# (web-desktop-unification slice 04). Same files/counts/reasons — relocation, not
# gate-weakening; product-client/src is now a check_max_lines scan root. Entries
# that fell under the 600 general cap at the deeper path (SplitDiffViewer 540,
# HomeNextScreen.test 530 — desktop component cap was 500) are dropped, not moved.
apps/packages/product-client/src/components/settings/panes/agents/harness/HarnessPane.test.tsx 1208 C-train composition: C-2 ack-status UI coverage joins the B-4 cut; P3 runtime catalog extends All-Models coverage + add-key modal flow + switch disambiguation + surface store mock + ProductHost test-host wiring + authenticated-without-cloud-compute BYOK gating cases + C3 model-snapshot status mock stub + review-fix refresh mutation stub + C1 cursor api_key/CLI method + CURSOR_API_KEY persistence cases; F-040 dropped the client-refresh-with-payload cases (no callable cloud refresh remains); agent-auth.md §5 provider-picker two-write assertion (vault api_key + one selection row) replaces the prefill-only case; the §1–§7 pane rebuild adds the collapsed-model-list expansion helper; review-fix non-orphaning provider-picker cases (PUT-failure revoke + rollback, close-only-after-PUT, bound-env-var pass-through, key-shaped env var for multi-field providers); PR-review fixes add the P0 anonymous-local Authenticate-gate regression, the P1-a supportsLogin&&canRunLogin regression, and the P1-b gateway-capabilities-pending loading-state regression; design-handoff v2 rewrite (cards + merged header badge + providers section + saved-keys segment)
apps/packages/product-client/src/components/home/screen/HomeNextScreen.test.tsx 769 home screen suite: unified-composer/target-persistence coverage joined by the PRO-165 home-attachment battery (+ button snapshot payload, whole-screen drop, dropped-path local_ref recovery, local/remote target-flip clearing, attachment-only launch, failure retention, chip removal)
apps/packages/product-client/src/lib/domain/workspaces/cloud/logical-workspaces.test.ts 840 managed sandbox cloud workspace indicators + PR 5 explicit-materialization attachment cases extend existing oversized test file
apps/packages/product-client/src/lib/domain/workspaces/sidebar/sidebar.test.ts 687 workspace git-status threading extends existing oversized test file; R7 archiving-train hide-set deletion (§3.10) shrinks it further (archivedIds fixture removed)
apps/packages/product-client/src/lib/infra/measurement/measurement-port.ts 734 product-owned measurement port barrel (R1 ruling) mirroring the retained measurement surface through a swappable no-op sink; split deferred
apps/packages/product-client/src/domain/chats/cloud/transcript-view.test.ts 1160 relocated existing oversized domain test; no growth
apps/packages/product-client/src/domain/chats/transcript/transcript-row-model.test.ts 1363 relocated existing oversized domain test; +workspace-creation-receipt row coverage incl. after-first-user-message placement cases (composer-dock-receipt); receipt folded into first turn's Worked-for group; +frontier-hosting coverage on the last pending/outbox prompt row now that the standalone workspace_receipt row kind is gone (composer-dock-receipt placement fix)
apps/packages/product-client/src/domain/chats/transcript/transcript-row-model.ts 1017 relocated existing oversized domain source; receipt folded into first turn's Worked-for group; the standalone workspace_receipt row kind is deleted — before any turn exists the receipt instead hosts via hostsWorkspaceReceipt on the last pending/outbox prompt row (composer-dock-receipt placement fix)
apps/packages/product-client/src/domain/chats/transcript/transcript-presentation.test.ts 864 relocated existing oversized domain test; legacy Subagents MCP cases removed in PR10
apps/packages/product-client/src/domain/workspaces/cloud-work-inventory.test.ts 859 relocated existing oversized domain test; no growth
apps/packages/product-client/src/domain/sessions/activity.test.ts 660 relocated existing oversized domain test; no growth
apps/packages/product-client/src/domain/sessions/intents/session-intent.test.ts 659 PRO-261 queued-intent echo regression test
anyharness/crates/anyharness-lib/src/app/mod.rs 675 AnyHarness app wiring (+gateway model planner wiring; the gateway model resolver + probe-engine store wiring is gone with the old-chain deletion, A9) (+session mutation admission purge/retention wiring, +goal/loop/activity/feed services, +two-phase workflow-run and workflow-workspace wiring, +worktree restore runtime wiring; local materialization service wiring is now the materialization runtime valve, moved to app/materialization.rs per grid PR 7; -3: WorkspaceService construction/pass-through deleted, grid PR 8; net 0 in grid PR 9: WorkspaceRetireService joins AppState while the preflight-checker/purge/retire constructions move to the app/workspaces.rs wiring family); PR #1753 extracts process/runtime configuration helpers to app/config.rs while adding Workspace MCP composition; PR #3 moves Agent Operations graph construction to app/agent_operations.rs; PR10 removes legacy Subagents MCP wiring while adding live product-context composition ; -24: R1 archiving unwires WorkspaceRetireService and WorkspaceRetentionService (fields, constructions, the retention startup pass and the WorktreeRetentionPolicyStore import) ; +12: R4 archiving wires WorkspaceArchiveService (import, field, deps pass-through, construction, and the cfg(not(test)) sweep boot pass) ; +7 net: R5 wires QuiescePlanes and the deletion-family purge construction while the clippy fix drops the dead session_delete_workflow argument ; -10: workflows gen-2 PR1 unwires the gen-1 two-phase workflow-run and workflow-workspace services ; +26: PR4 wires the WorkflowManager (construction, extension registration, boot fence pass) ; +1: R5 (Update Flow ADR, FR-1) swaps CatalogSyncService::from_bundled() for the boot-fetch-aware from_bundled_and_staged_via_env(&runtime_home)
anyharness/crates/anyharness-lib/src/domains/agents/catalog/service_tests.rs 605 B-2 validation-cut rewrote the refusal assertions in place (single SESSION_MODEL_UNSUPPORTED); shrinks when the legacy gateway-models routes delete (C-track cutover)
anyharness/crates/anyharness-lib/src/domains/loops/runtime.rs 802 loops runtime — native write path + emulated Codex scheduler (+2b derived-safe classification comment at the fire seam; fire executor moved to its owning module in PR5)
anyharness/crates/anyharness-lib/src/live/sessions/actor/startup.rs 650 session startup (+goal/loop capability parsing, roster reconcile-on-attach); R3: +1 line initializing the new pending_stop_response field on SessionActor construction; observability PR9: +2 session.established record fields ; +5: pr11b threads the interaction hooks into the event sink at construction
anyharness/crates/anyharness-lib/src/live/sessions/model.rs 606 live-session shared model: actor messages, hooks, capabilities, event persist; split per guides/domains.md on next growth ; +7: pr11b adds the interaction-requested/resolved hook fields to SessionHooks
anyharness/crates/anyharness-lib/src/live/sessions/actor/tests/workspace_stop.rs 841 R3 real-process stop suite; every case owns a real agent process and a real ACP duplex, so the per-case setup is genuinely irreducible. +130: the dropped-stop re-entry regression (a stop future abandoned mid-escalation must leave the session answerable)
server/tests/integration/test_organizations_api.py 624 added admin-only coverage for GET .../invitations
server/proliferate/db/store/organizations.py 777 D1 adds get_default_organization_for_user (earliest active membership = signup placement org, the v1 payer law); SG09 review restores the collision-ladder and race-backstop rationale; SSO JIT membership persistence is relocated from auth_sso into its Organization store owner; split pending with the org-store taxonomy pass
server/proliferate/server/cloud/agent_gateway/enrollment.py 636 C-train composition re-attaches C-1 both-surface sync-completion pokes; D train: org-only ensure_signup_enrollment + per-(org,member) LiteLLM identity + fingerprint-driven per-key re-mint; shrank again when ensure_user_enrollment died (D-3), split pending
server/proliferate/server/cloud/materialization/materialize/agent_auth.py 725 C-train composition: C-1 ensure-on-switch + C-2 delivery-ack stamping join the cut; D3 python arm adds the provider_config render branch + per-(harness,config_kind) translation table (_translate_provider_config_env) + typed-vault loading in _load_state_inputs; review fix cycle 1 hardens _hostname_first_label's userinfo/port stripping; review fix cycle 2 repairs the dangling Foundry-mapping docstring; integration adds A4 fail-closed + B3 per-harness key map on the same module D1 fail-closed comments: the renderer withholding predicate now names unfunded subjects explicitly (D train)
server/tests/integration/test_agent_gateway_enrollment.py 887 C-train composition re-lands the C-1 poke coverage adapted org-only; D train rewrote the suite org-first (D1 proof, floor mirroring, signup shape); C-1 added the both-surfaces poke coverage
server/tests/integration/test_agent_gateway_migration.py 619 NEW in D-3: the legacy-enrollment migration proofs (D6) + the org-only grep-gates (D8)
server/tests/integration/test_agent_gateway_usage_credits.py 782 D train: D2 free-credit dedupe proofs + converted-claim guard + importer tests re-cut to the signup shape
server/tests/unit/test_agent_auth_materialization.py 990 C-train composition: C-1/C-2 renderer additions; renderer unit suite (+fail-closed rewrites, +the renderer-merge tripwire; the contract-fixture producer class split out to test_agent_auth_state_contract_fixture.py); D3 python arm adds TestTranslateProviderConfigEnv (6-row table) + TestRenderProviderConfigSource coverage for the new provider_config wire source; review fix cycle 1 adds a userinfo/port hostname-derivation edge case
server/proliferate/server/cloud/agent_gateway/service.py 813 delivery-ack hardening (server-bounded ack revision + trust-boundary docs) nudged the gateway service over the 800 server-layer cap; split pending with the service taxonomy pass; typed-config write gate threads the registry providerConfig vocabulary into the store put and pins R5's azure field set
anyharness/crates/proliferate-diagnostics-collector/src/bin/proliferate-diagnostics-rss-profile.rs 1099 RSS-ceiling proof harness for the collector: one sequential measurement run whose phases (spawn, warmup health, saturating ingest, tail handshake, RSS sampling, binary/profile hashing) share 62 interdependent locals and converge on a single Evidence record that is hashed and published as the proof artifact. Splitting it means inventing a phase-state struct, and the result can only be re-verified by a release build on a macOS runner, so it is pinned at its landed size rather than restructured blind. Split into src/bin/proliferate-diagnostics-rss-profile/ (main + child + evidence + phases) when a second measurement phase lands.
anyharness/crates/proliferate-diagnostics-collector/tests/collector_process.rs 1988 collector process-boundary suite: 17 tests that each spawn a real collector child over one shared harness (child lifecycle, loopback auth, oversized-body refusal, wire admission, graceful shutdown, RSS ceiling, deterministic query order, golden export, lifecycle retry/orphan exactness, producer cardinality, runtime slots, eviction/tail lag, boundary caps and restart loss, inherited capability, saturated-lifecycle degradation, saturated-lifecycle shutdown terminal, stalled-body permit release). The harness is the reason it is one file; the tests are independent. Split into tests/collector_support/mod.rs + per-topic test binaries when the next process-boundary test lands.

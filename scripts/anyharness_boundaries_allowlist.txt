# AnyHarness boundary debt allowlist.
#
# Format:
# RULE_ID path count reason
#
# This is migration debt, not permission for new code. If a cleanup reduces a
# count, update this file in the same PR.

API_LIVE_RUNTIME_IMPORT anyharness/crates/anyharness-lib/src/api/http/sessions.rs 1 transitional permission decision type still lives under live ACP runtime
SESSION_STORE_LIVE_IMPORT anyharness/crates/anyharness-lib/src/sessions/store/events.rs 1 transitional session event sanitizer still lives under ACP runtime
SESSION_STORE_LIVE_IMPORT anyharness/crates/anyharness-lib/src/sessions/store/notifications.rs 1 transitional raw notification sanitizer still lives under ACP runtime

# AnyHarness measured size debt (PROD-SIZE-1). A ratchet, not an exception
# ledger: max_lines is the observed count allowed for this file and may only
# shrink. Growth fails; so does a stale entry whose file is now under its
# allowance or gone. scripts/check_max_lines.py is the only reader.

[[max_lines]]
path = "anyharness/crates/anyharness-contract/src/v1/events.rs"
max_lines = 1092
reason = "existing AnyHarness contract oversized file"

[[max_lines]]
path = "anyharness/crates/anyharness-contract/src/v1/sessions.rs"
max_lines = 779
reason = "existing AnyHarness contract oversized file (+caller-selected idempotent create id)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/api/http/workspaces_purge.rs"
max_lines = 664
reason = "workspace purge handlers (+2b fail-closed all-session admission, PR1227-WORKSPACE-FENCE-01/02 admitted-set carry + under-exclusive-lease re-check, test-only purge proof seam module)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/api/openapi.rs"
max_lines = 692
reason = "existing AnyHarness oversized file (+hosting schema registration, +agent-auth state response, +gateway catalog endpoints) + enriched gateway-models schema fields (model-table §1) + workflow-runs + local materialization operations + workflow-run-workspaces paths/schemas + agent install progress schemas + worktree restore path/schemas + WorktreeBaseFetch + RuntimeDiskPressure schemas (central OpenAPI registry; per-route path/schema registration only)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/api/router.rs"
max_lines = 640
reason = "AnyHarness router (+goals/loops/activity/feed routes, +gateway catalog version route, +workflow-runs/cancel + local materialization operations + workflow-run-workspaces routes + worktree restore route)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/api/router_tests.rs"
max_lines = 1898
reason = "existing AnyHarness oversized test file (+hosting PR-status route tests, +catalog version tests, +managed default-placement route test, +model-snapshot status route shape/auth tests, +agent-auth/install poke contract tests, +agent-auth/install poke-suppression proofs against the automatic_poke_engine seam (F-036 cycle-2 review item 1); the catalog-push handler tests are gone with the heartbeat transport)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/api/session_admission_tests.rs"
max_lines = 650
reason = "2b admission conflict-matrix + fail-closed purge/retire/mobility-export/destroy-source proofs + LOCK-01 permit-before-lease deadlock/source-order guards (+idempotent create); -24: the retire LOCK-01 anchor split into two composing halves when grid PR 9 moved the state machine into domains/workspaces/retire.rs, the per-handler anchor rows collapsed into one table, and the 25-field SessionRecord fixture folded into the shared app/test_support helper"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/api/workflow_runs_tests.rs"
max_lines = 2790
reason = "workflow-runs HTTP + runtime/extension crossing suite (run-control PROOF-01 battery + 2b admission races: reservation/create/bind window, terminal release, PR1227-WORKSPACE-FENCE-01 purge/retire destruction-vs-late-bind proofs, and FENCE-02 bind->terminalize->destruction race proofs over the real executor)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/app/mod.rs"
max_lines = 691
reason = "AnyHarness app wiring (+gateway model planner wiring; the gateway model resolver + probe-engine store wiring is gone with the old-chain deletion, A9) (+session mutation admission purge/retention wiring, +goal/loop/activity/feed services, +two-phase workflow-run and workflow-workspace wiring, +worktree restore runtime wiring; local materialization service wiring is now the materialization runtime valve, moved to app/materialization.rs per grid PR 7; -3: WorkspaceService construction/pass-through deleted, grid PR 8; net 0 in grid PR 9: WorkspaceRetireService joins AppState while the preflight-checker/purge/retire constructions move to the app/workspaces.rs wiring family); 691 = 690 + a rustfmt wrap of the materialization wiring head (module-qualified deps struct exceeds max_width by 3; collapsing needs a sub-4-char name or a convention-breaking unqualified import that nets the same — documented in PR #1673)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/auth/context_tests.rs"
max_lines = 645
reason = "classifier tests incl. gateway route activation + bedrock flag-only detection"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/auth/launch_facts.rs"
max_lines = 657
reason = "Track D provider_config credential facts: a source's already-resolved env map classifies per-key against the registry's declared flag vocabulary (registry_flag_vars), producing EnvFlag for mode-switch flags (e.g. CLAUDE_CODE_USE_BEDROCK) and presence-only Env for everything else, same secrets rule as api_key; the vocabulary unions auth.slots[] with providerConfig[] (D3-rust)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/catalog/projection.rs"
max_lines = 998
reason = "family normalizer (bedrock/date/version forms) with tests (+native-default-model resolution and its tests, incl. the opencode any-non-gateway-fallback pin), plus the enrichment join (own vs foreign catalog match) and its tests relocated from agent_gateway_catalog.rs under A9's route-backend swap (ruling §3): gateway_resolver.rs is deleted (old resolver chain, its bedrock_default_model catalog-default resolution now lives in gateway_plan.rs), and the route file itself dropped to 312 lines and no longer needs an allowlist entry"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/catalog/service_tests.rs"
max_lines = 605
reason = "B-2 validation-cut rewrote the refusal assertions in place (single SESSION_MODEL_UNSUPPORTED); shrinks when the legacy gateway-models routes delete (C-track cutover)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/catalog/validation.rs"
max_lines = 609
reason = "existing catalog validator router; unattended-mode invariants live in a focused submodule"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/model.rs"
max_lines = 605
reason = "agents domain type barrel, previously exactly at the cap (+ResolvedAgent.credentials_from_route, the route-vs-native readiness provenance flag); split into model/ on next growth per guides/domains.md"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/route_auth/render.rs"
max_lines = 825
reason = "Track D provider_config render arm (claude/opencode generic env-set, codex aws_bedrock via built-in amazon-bedrock provider, codex azure_openai UNVERIFIED config.toml injection kept behind the registry's pending flag, cursor/grok/unknown-kind UnsupportedRoute) landed alongside the existing native/gateway recipe table, +sanitize_claude_ambient fix so a provider_config route's own rerouting flag (e.g. CLAUDE_CODE_USE_BEDROCK) survives sanitization instead of being unconditionally stripped, scoped in review cycle 1 to the keys the provider_config arm itself recorded so an api_key row named like a rerouting flag is still stripped (D3-rust)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/cowork/runtime.rs"
max_lines = 1893
reason = "existing AnyHarness oversized file (catalog-owned unattended default replaces local fallback helper)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/goals/service_tests.rs"
max_lines = 682
reason = "goal service regression tests for pending_op idempotency"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/loops/runtime.rs"
max_lines = 867
reason = "loops runtime — native write path + emulated Codex scheduler (+2b derived-safe classification comment at the fire seam)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/mobility/service.rs"
max_lines = 851
reason = "existing AnyHarness oversized file (shrunk from 1520: the live-reaching use cases moved to domains/mobility/runtime/, grid PR 6a)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/plans/service.rs"
max_lines = 607
reason = "existing AnyHarness oversized file; shrank from 628 after folding plans/plan_interaction_links reads into PlanStore (grid plan PR 5b)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/reviews/service/service_tests.rs"
max_lines = 1510
reason = "existing AnyHarness oversized test file"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/sessions/runtime/startup.rs"
max_lines = 637
reason = "session-startup runtime (+A9 Scope C: resolve_launch_agent status check at the common live-start seam, mirroring create_session's readiness gate, plus its three error-mapper arms; +review-fix cycle 1: the route-auth selection pre-check now runs before that status check, matching create_session's order)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/sessions/runtime/tests.rs"
max_lines = 1019
reason = "existing AnyHarness oversized test file (launch-env tests moved to their owning module) + A9 Scope C resume/fork revoked-credentials regression tests (AgentNotReady at the common live-start seam) + a review-fix pin (unsatisfiable selection reports RouteAuth, not AgentNotReady, on resume)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workflows/service.rs"
max_lines = 664
reason = "workflow durable rules (+run-control cancel-intent use case and helpers); split into service/ on next growth per guides/domains.md"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workflows/service_tests.rs"
max_lines = 1320
reason = "workflow-runs merge-gated domain suite (+2b partial-uniqueness contract on nonterminal session binding)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workspaces/inventory.rs"
max_lines = 641
reason = "runtime pressure worktree inventory joins and tests (test repo-root seed folded into the shared repo_roots test_support helper)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workspaces/retention.rs"
max_lines = 823
reason = "worktree retention sweep (+PR1227-RETENTION-FENCE-01 permit/lease fencing + test proof seam, +Workflow creator-context exclusion until an explicit cleanup policy exists)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workspaces/retire_preflight.rs"
max_lines = 613
reason = "existing AnyHarness oversized file"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/live/sessions/actor/startup.rs"
max_lines = 642
reason = "session startup (+goal/loop capability parsing, roster reconcile-on-attach)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/live/sessions/probe.rs"
max_lines = 679
reason = "catalog probe with per-harness model-source fallbacks (+the three runtime-probe options: env removal, scratch workspace root, config-option-only switch suppression); shrank from 683 after the readiness resolve moved to the call boundary (grid plan PR 5c/7 — live never fetches)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/persistence/custom_migrations.rs"
max_lines = 1126
reason = "custom migration with legacy schema coverage (+0063 partial unique controller index, ordered after the 0062 rebuild)"

[[max_lines]]
path = "anyharness/sdk-react/src/hooks/sessions.ts"
max_lines = 723
reason = "existing SDK React oversized file"

[[max_lines]]
path = "anyharness/sdk/src/client/core.ts"
max_lines = 603
reason = "AnyHarness TS client core (+preserved FastAPI detail body on AnyHarnessError for gateway billing-block classification, C8)"

[[max_lines]]
path = "anyharness/sdk/src/reducer/__tests__/transcript.test.ts"
max_lines = 1817
reason = "existing SDK oversized test file"

[[max_lines]]
path = "anyharness/sdk/src/reducer/transcript.ts"
max_lines = 1528
reason = "existing SDK oversized file"

# AnyHarness measured size debt (PROD-SIZE-1). A ratchet, not an exception
# ledger: max_lines is the observed count allowed for this file and may only
# shrink. Growth fails; so does a stale entry whose file is now under its
# allowance or gone. scripts/check_max_lines.py is the only reader.

[[max_lines]]
path = "anyharness/crates/anyharness-contract/src/v1/events.rs"
max_lines = 1087
reason = "existing AnyHarness contract oversized file"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/api/openapi.rs"
max_lines = 681
reason = "existing AnyHarness oversized file (+hosting schema registration, +agent-auth state response, +gateway catalog endpoints) + enriched gateway-models schema fields (model-table §1) + workflow-runs + local materialization operations + workflow-run-workspaces paths/schemas + agent install progress schemas + worktree restore path/schemas + WorktreeBaseFetch + RuntimeDiskPressure schemas (subagent registrations extracted to their owned OpenAPI document) ; -10: R1 archiving deletes the three retire path registrations, the three worktree-retention path registrations and the three WorkspaceRetire* schema registrations ; +22: R4 archiving registers the two lifecycle paths and the fourteen lifecycle schemas ; -4: R5 deletes the purge-preflight/retry path registrations and the WorkspaceCleanupOperation/WorkspacePurgePreflightResponse schema registrations ; -30: workflows gen-2 PR1 deletes the gen-1 workflow-runs and workflow-run-workspaces path/schema registrations ; +2: PR5a merges the workflow-runs OpenAPI document into the composed doc ; +16: agent-auth canonical evidence model registers the thirteen AgentAuth* schemas ; +4: slice 6a registers the two native-bridge paths, the NativeBridgeResponse schema, and its import"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/api/router_tests.rs"
max_lines = 1904
reason = "existing AnyHarness oversized test file (+hosting PR-status route tests, +catalog version tests, +managed default-placement route test, +model-snapshot status route shape/auth tests, +agent-auth/install poke contract tests, +agent-auth/install poke-suppression proofs against the automatic_poke_engine seam (F-036 cycle-2 review item 1); the catalog-push handler tests are gone with the heartbeat transport)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/app/mod.rs"
max_lines = 647
reason = "AnyHarness app wiring (+gateway model planner wiring; the gateway model resolver + probe-engine store wiring is gone with the old-chain deletion, A9) (+session mutation admission purge/retention wiring, +goal/loop/activity/feed services, +two-phase workflow-run and workflow-workspace wiring, +worktree restore runtime wiring; local materialization service wiring is now the materialization runtime valve, moved to app/materialization.rs per grid PR 7; -3: WorkspaceService construction/pass-through deleted, grid PR 8; net 0 in grid PR 9: WorkspaceRetireService joins AppState while the preflight-checker/purge/retire constructions move to the app/workspaces.rs wiring family); PR #1753 extracts process/runtime configuration helpers to app/config.rs while adding Workspace MCP composition; PR #3 moves Agent Operations graph construction to app/agent_operations.rs; PR10 removes legacy Subagents MCP wiring while adding live product-context composition ; -24: R1 archiving unwires WorkspaceRetireService and WorkspaceRetentionService (fields, constructions, the retention startup pass and the WorktreeRetentionPolicyStore import) ; +12: R4 archiving wires WorkspaceArchiveService (import, field, deps pass-through, construction, and the cfg(not(test)) sweep boot pass) ; +7 net: R5 wires QuiescePlanes and the deletion-family purge construction while the clippy fix drops the dead session_delete_workflow argument ; -10: workflows gen-2 PR1 unwires the gen-1 two-phase workflow-run and workflow-workspace services ; +26: PR4 wires the WorkflowManager (construction, extension registration, boot fence pass) ; +1: R5 (Update Flow ADR, FR-1) swaps CatalogSyncService::from_bundled() for the boot-fetch-aware from_bundled_and_staged_via_env(&runtime_home) ; -10: Lane H restores the workspaces wiring family for checkpoint-before-session plus archive/purge-after-session composition"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/auth/context_tests.rs"
max_lines = 645
reason = "classifier tests incl. gateway route activation + bedrock flag-only detection"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/auth/launch_facts.rs"
max_lines = 625
reason = "Track D provider_config credential facts: a source's already-resolved env map classifies per-key against the registry's declared flag vocabulary (registry_flag_vars), producing EnvFlag for mode-switch flags (e.g. CLAUDE_CODE_USE_BEDROCK) and presence-only Env for everything else, same secrets rule as api_key; the vocabulary unions auth.slots[] with providerConfig[] (D3-rust)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/route_auth/render.rs"
max_lines = 679
reason = "Track D provider_config render arm (claude/opencode generic env-set, codex aws_bedrock via built-in amazon-bedrock provider, codex azure_openai UNVERIFIED config.toml injection kept behind the registry's pending flag, cursor/grok/unknown-kind UnsupportedRoute) landed alongside the existing native/gateway recipe table, +sanitize_claude_ambient fix so a provider_config route's own rerouting flag (e.g. CLAUDE_CODE_USE_BEDROCK) survives sanitization instead of being unconditionally stripped, scoped in review cycle 1 to the keys the provider_config arm itself recorded so an api_key row named like a rerouting flag is still stripped (D3-rust)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/cowork/runtime.rs"
max_lines = 1889
reason = "existing AnyHarness oversized file (catalog-owned unattended default replaces local fallback helper)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/goals/service_tests.rs"
max_lines = 676
reason = "goal service regression tests for pending_op idempotency"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/loops/runtime.rs"
max_lines = 706
reason = "loops runtime — native write path + emulated Codex scheduler (+2b derived-safe classification comment at the fire seam; fire executor moved to its owning module in PR5; unit tests split to runtime_tests.rs to fit the extension-response decode diagnostic)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/plans/service.rs"
max_lines = 604
reason = "existing AnyHarness oversized file; shrank from 628 after folding plans/plan_interaction_links reads into PlanStore (grid plan PR 5b)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/reviews/service/service_tests.rs"
max_lines = 1491
reason = "existing AnyHarness oversized test file"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/sessions/runtime/tests.rs"
max_lines = 1158
reason = "existing AnyHarness oversized test file (launch-env tests moved to their owning module) + A9 Scope C resume/fork revoked-credentials regression tests (AgentNotReady at the common live-start seam) + a review-fix pin (unsatisfiable selection reports RouteAuth, not AgentNotReady, on resume) + Forks ADR rung 2 targeted-fork plumbing tests (item-less rejection, fail-closed no-child downgrade guard, idempotency conflict, unknown-outcome redispatch block); the same-payload resume lives in checkpoint_linkage_tests.rs with the Lane H NULL-linkage pins, and the rung-3 provider-anchor gates plus shared fixtures live in fork_anchor_gate_tests.rs"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/live/sessions/actor/tests/workspace_stop.rs"
max_lines = 840
reason = "R3 real-process stop suite; every case owns a real agent process and a real ACP duplex, so the per-case setup is genuinely irreducible. +130: the dropped-stop re-entry regression (a stop future abandoned mid-escalation must leave the session answerable)"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/live/sessions/probe.rs"
max_lines = 609
reason = "catalog probe with per-harness model-source fallbacks (+the three runtime-probe options: env removal, scratch workspace root, config-option-only switch suppression); shrank from 683 after readiness resolution and config-option model switching moved to owned seams"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/live/terminals/manager_tests.rs"
max_lines = 728
reason = "R3 workspace-stop terminal suite: +615 lines of stop_all_for_workspace coverage (kill census, setup-process reaping, wedged-terminal escalation) sharing one real-PTY manager harness per case"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/persistence/custom_migrations.rs"
max_lines = 1326
reason = "custom migration with legacy schema coverage (+0063 partial unique controller index, ordered after the 0062 rebuild) ; +110: R1 archiving adds the 0067_workspace_archived_lifecycle FK-off full-table rebuild ; +72: R5 adds the 0068_workspace_drop_cleanup_columns FK-off full-table rebuild ; +10: workflows gen-2 PR1 registers the 0069_workflow_runs_gen2 FK-off rebuild ; +8: fix/wf2-run-cancel registers the 0070_workflow_run_cancel_status FK-off rebuild"

[[max_lines]]
path = "anyharness/crates/proliferate-diagnostics-client/src/fallback/tests.rs"
max_lines = 736
reason = "Fallback writer suite: every test drives one writer through the same rotation and live-authority harness (injected write errno, short writes, rotation, retirement, consecutive-failure retirement), so the shared harness is the reason it is one file and the tests themselves are independent. Split into fallback/tests/{write,rotation,authority}.rs when the next injection seam lands."

[[max_lines]]
path = "anyharness/crates/proliferate-diagnostics-client/src/fallback/unix.rs"
max_lines = 651
reason = "Unix fallback writer: segment rotation, live-authority validation, partial-line rollback, and bounded interrupt-retrying writes all mutate the same file handle and byte counters under one lock, so the retry path cannot move without exporting that state. The cross-platform swap primitive already moved out to fallback/swap.rs; split the authority validation out next when a second platform needs it."

[[max_lines]]
path = "anyharness/crates/proliferate-diagnostics-collector/src/bin/proliferate-diagnostics-rss-profile.rs"
max_lines = 1099
reason = "RSS-ceiling proof harness for the collector: one sequential measurement run whose phases (spawn, warmup health, saturating ingest, tail handshake, RSS sampling, binary/profile hashing) share 62 interdependent locals and converge on a single Evidence record that is hashed and published as the proof artifact. Splitting it means inventing a phase-state struct, and the result can only be re-verified by a release build on a macOS runner, so it is pinned at its landed size rather than restructured blind. Split into src/bin/proliferate-diagnostics-rss-profile/ (main + child + evidence + phases) when a second measurement phase lands."

[[max_lines]]
path = "anyharness/crates/proliferate-diagnostics-collector/tests/collector_process.rs"
max_lines = 1988
reason = "collector process-boundary suite: 17 tests that each spawn a real collector child over one shared harness (child lifecycle, loopback auth, oversized-body refusal, wire admission, graceful shutdown, RSS ceiling, deterministic query order, golden export, lifecycle retry/orphan exactness, producer cardinality, runtime slots, eviction/tail lag, boundary caps and restart loss, inherited capability, saturated-lifecycle degradation, saturated-lifecycle shutdown terminal, stalled-body permit release). The harness is the reason it is one file; the tests are independent. Split into tests/collector_support/mod.rs + per-topic test binaries when the next process-boundary test lands."

[[max_lines]]
path = "anyharness/sdk-react/src/hooks/sessions.ts"
max_lines = 675
reason = "existing SDK React oversized file (subagent queries and lifecycle mutations extracted to their owned hook module)"

[[max_lines]]
path = "anyharness/sdk/src/reducer/__tests__/transcript.test.ts"
max_lines = 1721
reason = "existing SDK oversized test file; legacy Subagents MCP reducer coverage removed in PR10"

[[max_lines]]
path = "anyharness/sdk/src/reducer/transcript.ts"
max_lines = 1515
reason = "existing SDK oversized file; legacy Subagents MCP reducer branch removed in PR10"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workflows/store.rs"
max_lines = 1379
reason = "workflows gen-2 durable store: run/node/doc writes, apply_transition and projections share one SQLite seam; split into store/ on next growth per guides/domains.md ; +62: PR3 adds the docs-registry register/read seam and rendered-envelope persistence ; +32: PR4 adds the Ruling L DisposeThenStart side effect, the node model column, and queue-aware hold event emission ; +115: PR5a adds the courier PUT upsert (Ruling B one-live-run refusal, key-sorted definition JSON note) and the node_membership 404 discriminator read ; +11: pr11 adds the failure_code classification to the named transition event ; +20: pr11b splits the queued-interjection Hold out of the stale-notification target ; +25: fix/wf2-run-cancel adds the run-cancel command ; +25: fix round 1 widens Cancel's disposal to every running row (chain + adhoc), NULLs the disposed node's session_id, and clears interruption_code on cancel"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workflows/store_tests.rs"
max_lines = 1683
reason = "workflows gen-2 store suite over real migrations (idempotent create, transition application, projections, boot fence, docs registry) ; +139: PR3 adds docs-registry and rendered-envelope round-trip proofs ; +31: PR5a adds one-live-run refusal and membership-discriminator proofs ; +79: pr11 adds the subscriber-capture proof for the named transition event ; +89: pr11b adds the shared capture harness plus interjection-held and node_launched join-key proofs ; +117: fix/wf2-run-cancel adds the cancel command store suite ; +98: fix round 1 adds the running-adhoc double-disposal proof and the interruption_code-cleared-on-cancel proof"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workflows/transition.rs"
max_lines = 774
reason = "workflows gen-2 pure transition table: the ADR's ruling rows and their guards in one match ; +27: PR4 adds the ForcedUnload stop reason (parks as app_shutdown) and Ruling L redo-from-running ; +18: fix/wf2-run-cancel adds the Cancel command and transition ; +18: fix round 1 widens Cancel's disposal to every running row (chain + adhoc), mirroring on_boot_fence's scan"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/workflows/transition_tests.rs"
max_lines = 1193
reason = "workflows gen-2 transition suite: one test per ADR table row plus staleness and illegal-command proofs ; +76: PR4 adds ForcedUnload-parking and Ruling L redo-from-running proofs ; +55: fix/wf2-run-cancel adds the cancel transition suite ; +32: fix round 1 adds the running-adhoc disposal proof and parameterizes the terminal-command wall over Completed/Failed/Cancelled"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/live/workflows/lifecycle_tests.rs"
max_lines = 1069
reason = "workflows gen-2 engine lifecycle suite: scripted-ACP agents drive real runs through the manager/actor (happy path, gate approve, flip, redo, undo, boot fence) with zero mocks of our machinery"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/adapters/git/operations/worktrees.rs"
max_lines = 602
reason = "rustfmt normalization 2026-08-27 (lint wiring): multi-line rewraps under the pinned 1.98.0 toolchain pushed the file over the cap — mechanical whitespace, not new debt; shrink with the next real split"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/agents/catalog/artifact.rs"
max_lines = 645
reason = "rustfmt normalization 2026-08-27 (lint wiring): multi-line rewraps under the pinned 1.98.0 toolchain pushed the file over the cap — mechanical whitespace, not new debt; shrink with the next real split"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/loops/service_tests.rs"
max_lines = 607
reason = "rustfmt normalization 2026-08-27 (lint wiring): multi-line rewraps under the pinned 1.98.0 toolchain pushed the file over the cap — mechanical whitespace, not new debt; shrink with the next real split"

[[max_lines]]
path = "anyharness/crates/anyharness-lib/src/domains/sessions/runtime/prompt_message_actor_tests.rs"
max_lines = 603
reason = "rustfmt normalization 2026-08-27 (lint wiring): multi-line rewraps under the pinned 1.98.0 toolchain pushed the file over the cap — mechanical whitespace, not new debt; shrink with the next real split"

[[max_lines]]
path = "apps/desktop/src-tauri/src/diagnostics/support_snapshot/coordinator/timestamp_tests.rs"
max_lines = 628
reason = "rustfmt normalization 2026-08-27 (lint wiring): multi-line rewraps under the pinned 1.98.0 toolchain pushed the file over the cap — mechanical whitespace, not new debt; shrink with the next real split"

[[max_lines]]
path = "apps/desktop/src-tauri/src/updater_owned.rs"
max_lines = 616
reason = "rustfmt normalization 2026-08-27 (lint wiring): multi-line rewraps under the pinned 1.98.0 toolchain pushed the file over the cap — mechanical whitespace, not new debt; shrink with the next real split"

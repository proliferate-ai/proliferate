# System manifest (Organization Standard rule 2) — validated by
# scripts/check_manifests.py; rules PROD-MANIFEST-001/002 in
# lints/product/manifests.toml. allowed_importers is measured reality, not
# permission-by-intention: the checker fails on drift in either direction.

name = "agent_auth"
spec = "specs/FEATURE_DOCS/AGENT_AUTH.md"
owns = "Agent credential selections, key vault, gateway enrollment, budgets and top-ups, and org agent-model policy: the agent-auth system's control-plane home."

public_surface = [
    "proliferate.server.agent_auth.api",
    "proliferate.server.agent_auth.service",
    "proliferate.server.agent_auth.models",
]

allowed_importers = [
    "accounts",
    "billing",
    "cloud",
    "cloud/materialization",
    "main.py",
    "organizations",
]

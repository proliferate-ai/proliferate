# System manifest (Organization Standard rule 2) — validated by
# scripts/check_manifests.py; rules PROD-MANIFEST-001/002 in
# lints/product/manifests.toml. allowed_importers is measured reality, not
# permission-by-intention: the checker fails on drift in either direction.

name = "organizations"
spec = "specs/systems/identity/organizations.md"
owns = "Organizations, membership, invitations, and organization-level policy."

public_surface = [
    "proliferate.server.organizations.api",
    "proliferate.server.organizations.service",
    "proliferate.server.organizations.models",
]

allowed_importers = [
    "accounts",
    "billing",
    "main.py",
    "setup",
    "integration_gateway",
    "seam",
]

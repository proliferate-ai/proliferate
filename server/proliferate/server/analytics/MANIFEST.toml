# System manifest (Organization Standard rule 2) — validated by
# scripts/check_manifests.py; rules PROD-MANIFEST-001/002 in
# lints/product/manifests.toml. allowed_importers is measured reality, not
# permission-by-intention: the checker fails on drift in either direction.

name = "analytics"
spec = "specs/engineering/observability/analytics.md"
owns = "Product analytics event ingestion endpoints on the control plane."

public_surface = [
    "proliferate.server.analytics.api",
    "proliferate.server.analytics.service",
    "proliferate.server.analytics.models",
]

allowed_importers = [
    "main.py",
]

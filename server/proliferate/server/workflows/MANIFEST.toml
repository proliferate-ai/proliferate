# System manifest (Organization Standard rule 2) — validated by
# scripts/check_manifests.py; rules PROD-MANIFEST-001/002 in
# lints/product/manifests.toml. allowed_importers is measured reality, not
# permission-by-intention: the checker fails on drift in either direction.

name = "workflows"
spec = "specs/FEATURE_DOCS/WORKFLOWS.md"
owns = "Server-side workflow definitions, invocations, and runs."

public_surface = [
    "proliferate.server.workflows.api",
    "proliferate.server.workflows.service",
    "proliferate.server.workflows.models",
    "proliferate.server.workflows.access",
]

allowed_importers = [
    "main.py",
]

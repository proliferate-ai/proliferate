# System manifest (Organization Standard rule 2) — validated by
# scripts/check_manifests.py; rules PROD-MANIFEST-001/002 in
# lints/product/manifests.toml. allowed_importers is measured reality, not
# permission-by-intention: the checker fails on drift in either direction.

name = "artifact_runtime"
spec = "specs/systems/sessions/cowork-artifacts.md"
owns = "Serving runtime for agent-produced artifacts (static bundle and file responses)."

public_surface = [
    "proliferate.server.artifact_runtime.api",
    "proliferate.server.artifact_runtime.service",
]

allowed_importers = [
    "main.py",
]

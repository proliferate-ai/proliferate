# System manifest (Organization Standard rule 2) — validated by
# scripts/check_manifests.py; rules PROD-MANIFEST-001/002 in
# lints/product/manifests.toml. allowed_importers is measured reality, not
# permission-by-intention: the checker fails on drift in either direction.

name = "integration_gateway"
spec = "specs/server/README.md"
owns = "Gateway-mediated integration tool access for agents: the integration-gateway system's gateway half."

public_surface = [
    "proliferate.server.cloud.integration_gateway.api",
    "proliferate.server.cloud.integration_gateway.service",
    "proliferate.server.cloud.integration_gateway.models",
]

allowed_importers = [
    "cloud",
    "cloud/integrations",
]

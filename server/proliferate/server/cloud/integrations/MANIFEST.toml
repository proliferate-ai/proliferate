# System manifest (Organization Standard rule 2) — validated by
# scripts/check_manifests.py; rules PROD-MANIFEST-001/002 in
# lints/product/manifests.toml. allowed_importers is measured reality, not
# permission-by-intention: the checker fails on drift in either direction.

name = "integration_gateway"
spec = "specs/server/README.md"
owns = "Product integration connections: OAuth flows, health probes, admission, and management surfaces — the integration-gateway system's connections half."

public_surface = [
    "proliferate.server.cloud.integrations.api",
    "proliferate.server.cloud.integrations.service",
    "proliferate.server.cloud.integrations.models",
    "proliferate.server.cloud.integrations.access",
]

allowed_importers = [
    "background",
    "cloud",
    "cloud/integration_gateway",
    "main.py",
]

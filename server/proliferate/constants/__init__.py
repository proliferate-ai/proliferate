"""Shared hardcoded constants used across multiple server modules.

Domain-specific files live alongside this ``__init__`` (e.g.
``constants/cloud.py``, ``constants/billing.py``).  File-private
implementation details should stay in the module that owns them until
they are genuinely shared across package boundaries.
"""

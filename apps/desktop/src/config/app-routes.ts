export const APP_ROUTES = {
  home: "/",
  plugins: "/plugins",
  automations: "/automations",
  workspaces: "/workspaces",
  settings: "/settings",
} as const;

export const LEGACY_APP_ROUTES = {
  powers: "/powers",
} as const;

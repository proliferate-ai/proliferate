/**
 * TEMPORARY launch gate: cloud compute surfaces (new cloud workspaces,
 * Migrate workspace, Enable remote access, Open in web) are disabled
 * broadly for this release. Identity — sign-in, account, billing — is
 * not affected.
 *
 * Flip to false to restore cloud compute.
 */
export const CLOUD_COMPUTE_TEMPORARILY_DISABLED = false;

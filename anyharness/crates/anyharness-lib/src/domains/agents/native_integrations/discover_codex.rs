//! Read-only discovery of codex native integrations: `~/.codex/config.toml`
//! `[mcp_servers.*]` and the bundled-plugin inputs the curated bundles need.
//! Spec: "Discovery". Filled in by lane L1; the barrier leaves it empty.

# Goals + Workflows — as-built architecture

**Moved:** the canonical as-built deep dive (file paths, flows, per-layer
maps, drift appendix) is **`codex/workflows-architecture.md`** — written
against the newer local tip `dfe38a81b` (origin/workflows/v1 + UI polish
rounds + the `agent.config` step + live goal progress), superseding the
earlier version of this file that was verified against `origin/workflows/v1`
(`2577b19cd`) only.

Quick facts carried there: PR stack #921 ← #909 ← sidecars #12/#24, merge
order #12/#24 → pin bump → #909 → #921, both-lane validation results
(2026-07-03), operational recipes (local stack, test commands, e2b
dev-template rebuild).

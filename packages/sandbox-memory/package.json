{
	"name": "@proliferate/sandbox-memory",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"bundle": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/sandbox-memory.cjs --external:better-sqlite3 --format=cjs",
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"better-sqlite3": "^11.0.0",
		"chokidar": "^4.0.0"
	},
	"devDependencies": {
		"@types/better-sqlite3": "^7.6.8",
		"esbuild": "^0.24.0",
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}

// Hosted-cloud V1 product kill switch. The server-side capability flags remain
// the source of truth for API enforcement; Desktop keeps BYOK setup hidden
// until there is a Cloud capability response for this surface.
export const AGENT_GATEWAY_BYOK_ENABLED = false;
